# 📋 Requirements — Dependencias del Proyecto FRAVEGA
#
# Instalar: pip install -r requirements.txt

# === HTTP Client (CRÍTICO — reemplazo de requests) ===
curl_cffi>=0.7.0          # Impersonación TLS/JA3 para bypass WAF

# === Data Processing ===
pandas>=2.0.0             # Análisis de datos y DataFrames
openpyxl>=3.1.0           # Lectura/escritura de archivos Excel (.xlsx)

# === Browser Automation (fallback cuando curl_cffi no alcanza) ===
playwright>=1.40.0        # Automatización de browser con stealth

# === Async (incluido en Python stdlib pero helpers útiles) ===
aiofiles>=23.0            # Async file I/O

# === Testing ===
pytest>=7.0               # Framework de testing
pytest-asyncio>=0.21      # Support async en pytest

# === Utilidades ===
python-dotenv>=1.0.0      # Cargar .env
rich>=13.0                # Output bonito en terminal
